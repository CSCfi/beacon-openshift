'use strict';

describe('beaconApp.view module', function() {

  beforeEach(module('beaconApp.view'));

  describe('beaconApp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});
